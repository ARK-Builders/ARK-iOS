# Core Bindings

This module contains Swift bindings for ARK Core.

`Sources/ArkDrop/ArkDrop.swift` is generated by UniFFI and committed so SwiftPM
consumers can resolve the package normally. Native binaries are generated into
`core/Artifacts` for local development and attached to versioned ARK-iOS
releases for distribution.
